# Web backend API contract — moved

The authoritative contract is now an immutable raw source document.

→ **[raw/web-backend-api.md](../../raw/web-backend-api.md)**

The navigable wiki summary lives at [wiki/entities/web-backend.md](../../wiki/entities/web-backend.md).

Reminder: when this contract conflicts with the actual web repo source files (`packages/server/src/{routes,controllers,services,db}/*`), the source files win — re-sync `raw/web-backend-api.md` from the web repo, do not edit it as a wiki page.
