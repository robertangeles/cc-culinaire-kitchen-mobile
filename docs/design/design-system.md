# Design system — moved

This document has moved into the LLM wiki.

→ **[wiki/entities/design-system.md](../../wiki/entities/design-system.md)**

The code source of truth for tokens (palette, type, spacing, radii, shadows, motion) is `src/constants/theme.ts`. The wiki page is the navigable summary; the original full reference is preserved in git history if you need the longer prose.
