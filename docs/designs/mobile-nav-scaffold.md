---
status: ACTIVE
---

# CEO Plan: Mobile Main Navigation Scaffold (placeholder feature destinations)

Generated by /plan-ceo-review on 2026-06-17
Branch: feature/api-pivot-backend-chat | Mode: SELECTIVE EXPANSION
Repo: robertangeles/cc-culinaire-kitchen-mobile

## Problem

Mobile dead-ends at two tabs (Chat, Settings) while the web app exposes a
12-item sidebar across 4 sections. We need a mobile navigation scaffold that
mirrors the web feature set so features can be ported in incrementally,
without re-architecting navigation each time. The placeholder screens are
disposable; the navigation PATTERN is the near-one-way-door decision.

Web sidebar mirrored:

- Chat Assistant (already the mobile Chat tab — no placeholder needed)
- CREATIVE LABS: Recipe Lab, Patisserie Lab, Spirits Lab
- KITCHEN OPERATIONS: My Recipe Book, Stock Room, Purchasing, Menu Intelligence,
  Kitchen Copilot, Waste Intelligence
- COMMUNITY: CulinAIre Recipe Book, The Bench

=> 11 placeholder destinations (Chat excluded).

## Chosen approach (0C-bis) — APPROACH A: "Kitchen" hub tab

Bottom tabs become `Chat | Kitchen | Settings`. The Kitchen tab renders a
scrollable grouped list mirroring the web sidebar (3 sections, 11 rows). Each
row routes to a placeholder screen. Chat stays the hero, one tap away. No new
navigation dependency (reuses @react-navigation/bottom-tabs + expo-router).

Elegance lever: a single nav config (source of truth) + one dynamic placeholder
route collapse 11 "screens" into 2 real files.

[D5 — route structure] The hub + placeholders are a NESTED STACK inside the
Kitchen tab (NOT a separate top-level `(kitchen)` group). This keeps the bottom
tab bar visible on placeholder screens, keeps everything under `(tabs)` (already
past the auth gate for in-app nav, so no RouteGuard edit for in-app), and gives
back navigation within the tab. Resolves the outside-voice catch that a sibling
`(kitchen)` group would drop the tab bar on every placeholder.

Rejected alternatives:

- B) Drawer/hamburger — closest 1:1 to web, but drawer nav is lower-engagement
  on mobile and adds @react-navigation/drawer + gesture surface.
- C) Bottom tabs + More — choosing which 4 tabs are "primary" is arbitrary
  while everything is a placeholder; gets reshuffled once real features land.

## Vision

### 10x Check

The Kitchen hub is not just a menu — it becomes the feature-parity ledger
between web and mobile. A typed nav config with a `status` field is the single
place that answers "what's live on mobile vs still web-only," drives the "Soon"
chips, and (later) gates deep-link entry points. Porting a feature is a
one-line status flip + swapping the placeholder for a real screen.

## Scope Decisions

| #        | Proposal                                                                                                                                      | Effort | Decision              | Reasoning                                                                                                                                                                                           |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Baseline | 3-tab nav + Kitchen hub + dynamic [slug] placeholder route + nav config + i18n + tests                                                        | M      | ACCEPTED              | Core scaffold; held scope                                                                                                                                                                           |
| E1       | `status: placeholder \| live` field + copper "Soon" chip on placeholder rows                                                                  | S      | ACCEPTED              | Honest signposting (design-for-trust) + built-in parity tracker                                                                                                                                     |
| E2       | "Open on web" CTA per placeholder (deep-link to web feature in browser)                                                                       | M      | SKIPPED               | Sends users off-app; reads as "mobile feature isn't real." Pure placeholder preferred for now                                                                                                       |
| E3       | Deep-link readiness — RouteGuard ack short-circuit for the `(tabs)/kitchen` sub-route (NOT per-slug registration; expo-router auto-registers) | S      | ACCEPTED (kept at D6) | Cold-launch deep links are swallowed by the food-safety ack gate (ack resets per cold launch); the guard short-circuit is the real work. Lets push / web "open in app" route to a feature once live |
| D5       | Route structure: nested stack inside the Kitchen tab vs separate `(kitchen)` group                                                            | S      | NESTED                | Keeps tab bar on placeholders; removes a RouteGuard copy-paste; absorbs outside-voice points 2 & 4                                                                                                  |
| D6       | E3 timing: keep in this PR vs defer to first real feature                                                                                     | —      | KEPT                  | User chose to wire deep-link readiness now                                                                                                                                                          |

## Accepted Scope (this plan)

- Bottom tabs: `Chat | Kitchen | Settings` (add Kitchen tab to (tabs)/\_layout.tsx).
- `src/constants/kitchenNav.ts` — typed nav config: 3 sections × 11 items
  (`slug`, `labelKey`, `icon`, `section`, `status: 'placeholder' | 'live'`).
  `icon` = Feather glyph name (matches existing tabs, `@expo/vector-icons/Feather`);
  confirm each of the 11 slugs maps to a real Feather glyph at build time.
  Single source of truth for the hub, the placeholder route, and deep links.
- [D5] `app/(tabs)/kitchen/_layout.tsx` (Stack, headerShown per editorial),
  `app/(tabs)/kitchen/index.tsx` → `src/components/kitchen/KitchenHubScreen.tsx`
  — grouped scroll list (Eyebrow section headers, row pattern, "Soon" chips).
  Add the Kitchen tab to `app/(tabs)/_layout.tsx` pointing at this stack.
- [D5] `app/(tabs)/kitchen/[slug].tsx` →
  `src/components/kitchen/PlaceholderScreen.tsx` — one dynamic route nested in
  the tab; resolves slug against the config; renders title/icon/section + empty
  state; unknown slug → graceful fallback (not a crash). Tab bar persists.
  - [D4] Empty state is the EDITORIAL treatment, NOT a generic "coming soon":
    paper card, copper accent, `Eyebrow` section label, feature name + a
    one-line on-brand note, styled from `theme.ts` tokens (no hex literals).
    Reference `ChatList`'s "Pick a Chef" empty state. Run `/plan-design-review`
    before build.
- [D3] Food-safety ack posture: the kitchen sub-route
  (`segments[0]==='(tabs)' && segments[1]==='kitchen'`) short-circuits the ack
  gate in RouteGuard. Only matters for cold-launch deep links (E3); in-app nav
  is already past the gate. Placeholders carry no Antoine content, so bypass is
  acceptable. TODO (below): re-gate any Kitchen screen that becomes AI-live.
- E1: `status` field + `SoonChip` (copper, theme tokens).
- E3 [KEPT — D6]: deep-link readiness. NOTE: expo-router already auto-registers
  every file route for the `ccculinairekitchenmob` scheme — no per-slug
  registration needed. With D5, the deep-link target is
  `/(tabs)/kitchen/<slug>`. In-app nav needs no guard change (Kitchen tab lives
  under `(tabs)`, past the gate once acked). The REAL work is the cold-launch
  deep-link path: `ackedThisSession` resets every cold launch
  (`app/_layout.tsx:54-57`), so a cold-launch deep link is swallowed by the
  food-safety ack gate (`app/_layout.tsx:93-99` → `(food-safety)`). Sub-scope:
  - [D3 — corrected at eng review] Short-circuit the ack gate for the kitchen
    sub-route: `if (segments[0] === '(tabs)' && segments[1] === 'kitchen') return;`
    placed **after** the auth + email-verify checks (`app/_layout.tsx:89`) and
    **before** the ack check (`:93`) — NOT alongside `(legal)`/`(feedback)` at
    `:64,71`. Those return before the `!user` check, so placing it there would
    let a logged-out deep link reach Kitchen (auth bypass). Correct placement:
    unauthed → `(welcome)`, unverified → verify-email, authed+verified → reaches
    the placeholder skipping ONLY food-safety. Bypass is acceptable because
    placeholders carry no Antoine content.
  - TODO (below): re-gate (require ack / resume-after-ack) any kitchen screen
    that becomes AI-backed and real (e.g. Kitchen Copilot).
- i18n: labels resolved via `t(labelKey)` (react-i18next, matching
  SettingsScreen), keys namespaced under `kitchen.*`. en + fr bundles both get
  all new keys: 11 labels, 3 section headers, empty-state copy, "Soon" chip,
  tab label. Missing-key safety: rely on i18next `fallbackLng: 'en'`.
- Tests (unit + integration): hub renders 11 rows under correct sections;
  tapping a row navigates to its slug; placeholder resolves a known slug;
  unknown slug renders fallback, does not crash; "Soon" chip shows only for
  `status: 'placeholder'`.
- **CRITICAL regression test (RouteGuard auth-state matrix)** — modifying
  RouteGuard changes existing auth behavior, so this is mandatory. Assert all
  three cold-launch deep-link paths into `/(tabs)/kitchen/<slug>`:
  (a) no user → redirected to `(welcome)`;
  (b) user, email unverified → redirected to `(auth)/verify-email`;
  (c) user, verified, ack NOT done this session → reaches the placeholder
  (food-safety bypassed). Proves the short-circuit relaxes ONLY the ack, never
  auth. File: `__tests__/integration/routeGuardKitchen.test.tsx`.

- [TODO-2 → built in this PR; mechanism corrected at eng review — D2 + D4]
  BUILD-TIME gate for store-review builds. NOT the server `featureFlagsService`
  (global + async + flickers — can't target one build). Instead:
  `EXPO_PUBLIC_KITCHEN_ENABLED` surfaced via `app.config.ts`, set false on the
  store-review EAS profile, read as a constant in `src/constants/config.ts`.
  Default true for internal/dev/production.
  [D4 — gate the room, not just the door] When the flag is false it must gate
  ALL THREE, or the routes leak (a hidden tab does NOT remove the registered
  `/(tabs)/kitchen/*` routes, and a deep link would still reach a placeholder
  AND trigger the ack-bypass):
  1. `app/(tabs)/_layout.tsx` — don't render the Kitchen `<Tabs.Screen>`.
  2. `app/(tabs)/kitchen/_layout.tsx` (or index + [slug]) — `<Redirect href="/(tabs)/chat" />`
     when `!KITCHEN_ENABLED`, so deep links can't reach the placeholders.
  3. RouteGuard — the kitchen ack short-circuit is itself guarded by
     `KITCHEN_ENABLED`, so the food-safety bypass cannot fire in a build where
     Kitchen is off. Resolves the T5 ↔ T7 conflict.
     Test: with the flag false, a deep link to `/(tabs)/kitchen/recipe-lab` lands
     on chat, NOT a placeholder, and the ack gate is NOT bypassed.

## Deferred to TODOS.md

- E2: "Open on web" CTA per placeholder — revisit once web URLs per slug are
  confirmed and we decide whether off-app hand-off is desired UX.
- [from D3] Re-gate food-safety ack (require ack / resume-after-ack) for any
  Kitchen screen that becomes AI-backed and real (e.g. Kitchen Copilot).
- [TODO-1 — SKIPPED] Placeholder tap analytics — decided not worth tracking;
  prioritize porting by product judgment.

## NOT in scope

- Making any placeholder a real feature (no backend reads, no real Recipe Book).
- Reordering / personalising the hub.
- iOS-specific nav chrome (Android is the primary device — Moto G86 Power).

## What already exists (reused, not rebuilt)

- `@react-navigation/bottom-tabs` + `expo-router` — already wired; Kitchen tab
  is an addition, not a new nav system.
- `app/(legal)/[slug].tsx` — proves the config-resolve + unknown-slug-fallback
  dynamic-route pattern in-repo. PlaceholderScreen follows it.
- `Eyebrow`, `CopperButton`, theme tokens, `ChatList` "Pick a Chef" empty state
  — reused for the editorial placeholder + hub.
- `src/services/featureFlagsService.ts` — reused for the store-build tab gate.
- react-i18next + en/fr bundles — reused for all new labels.

## Dream state delta

12-month ideal: every web feature has a native mobile screen; the nav config is
the live web↔mobile parity ledger. This plan lays the rails (config + hub +
status field) so each port is a status flip + a screen swap. It moves toward
the ideal; the only debt is per-feature (each port still needs its real screen).

## Implementation Tasks

Synthesized from this review's findings. Each task derives from a specific
finding above. Run with Claude Code; checkbox as you ship.

- [ ] **T1 (P1, human: ~1h / CC: ~10min)** — nav-config — Create typed `kitchenNav` config (slug, labelKey, Feather icon, section, status)
  - Surfaced by: Sec 1 Architecture — single source of truth for hub + route + deep links
  - Files: `src/constants/kitchenNav.ts`
  - Verify: `tsc` clean + unit test asserts 11 items across 3 sections
- [ ] **T2 (P1, human: ~1h / CC: ~15min)** — navigation — Add Kitchen tab + nested stack under `(tabs)/kitchen` (D5)
  - Surfaced by: D5 — nested stack keeps the tab bar, no RouteGuard edit for in-app
  - Files: `app/(tabs)/_layout.tsx`, `app/(tabs)/kitchen/_layout.tsx`, `app/(tabs)/kitchen/index.tsx`
  - Verify: 3 tabs render; hub lists 11 rows; tab bar persists on a placeholder
- [ ] **T3 (P1, human: ~2h / CC: ~20min)** — kitchen-hub — KitchenHubScreen: grouped list, Eyebrow headers, SoonChip (E1)
  - Surfaced by: E1 status field + Soon chip; Sec 11 editorial list
  - Files: `src/components/kitchen/KitchenHubScreen.tsx`, `src/components/kitchen/SoonChip.tsx`
  - Verify: 11 rows under 3 sections; Soon chip only when `status==='placeholder'`
- [ ] **T4 (P1, human: ~2h / CC: ~20min)** — placeholder — PlaceholderScreen + dynamic `[slug]` route, editorial card, unknown-slug fallback (D4)
  - Surfaced by: D4 editorial empty state; Sec 2 unknown-slug fallback
  - Files: `app/(tabs)/kitchen/[slug].tsx`, `src/components/kitchen/PlaceholderScreen.tsx`
  - Verify: known slug resolves; unknown slug → fallback, no crash; theme tokens only
- [ ] **T5 (P1, human: ~1h / CC: ~15min)** — route-guard — RouteGuard ack short-circuit for `(tabs)/kitchen` sub-route (E3/D3)
  - Surfaced by: E3 deep-link readiness + D3 ack bypass; ack resets per cold launch (`app/_layout.tsx:54-57`)
  - Files: `app/_layout.tsx`
  - Verify: cold-launch deep link to `/(tabs)/kitchen/recipe-lab` reaches the placeholder
- [ ] **T6 (P2, human: ~1h / CC: ~15min)** — i18n — Add `kitchen.*` keys to en + fr
  - Surfaced by: Sec 1 i18n via `t()`; `fallbackLng: 'en'`
  - Files: `src/locales/en.json`, `src/locales/fr.json`
  - Verify: `t(labelKey)` resolves; missing fr key falls back to en
- [ ] **T7 (P2, human: ~2h / CC: ~15min)** — feature-flag — Hide Kitchen tab for store-review builds (TODO-2)
  - Surfaced by: Sec 9 deploy — Play "incomplete functionality" review risk
  - Files: `src/services/featureFlagsService.ts`, `app/(tabs)/_layout.tsx`
  - Verify: tab hidden when flag off; visible internal/dev
- [ ] **T8 (P1, human: ~2h / CC: ~20min)** — tests — Unit + integration coverage
  - Surfaced by: Sec 6 test specs
  - Files: `__tests__/unit/kitchenNav.test.ts`, `__tests__/integration/kitchenHub.test.tsx`
  - Verify: `pnpm test` green — hub render, nav, slug resolve, Soon gating, guard pass
- [ ] **T9 (P3, gate)** — design — Run `/plan-design-review` on the placeholder + hub before build (D4)

## Design Spec (plan-design-review)

Classifier: APP UI (navigation, task-focused). Calibrated to `theme.ts` + the
"Editorial Design Standard" + the live `ChatGreeting` hero. Decisions:

**Kitchen hub (`KitchenHubScreen`)**

- [DD2] Each section is ONE grouped inset surface: `paperDeep` rounded
  container holding its rows, `paperEdge` hairline dividers between rows.
  `Eyebrow` (copperDeep, uppercase) header above each surface. Chef's-notebook
  table-of-contents feel. NOT a card-per-feature mosaic (hard-rejection #7).
- Row anatomy: leading Feather icon (`inkMuted`) + label (`type.ui`, `ink`) +
  trailing — `SoonChip` on placeholder rows, chevron (`inkFaint`) on live rows
  only. Min height 44pt (`layout.tap`).
- `SoonChip`: `copperDeep` text on `copperTint` fill, small pill, `type.overline`.
  No left-border accent (slop #8), no emoji (slop #7). Contrast ≥ 4.5:1.
- Hub title "Kitchen" rendered inline in Fraunces (own header, not native).
- a11y: each row `accessibilityRole="button"`, `accessibilityLabel` e.g.
  "Recipe Lab, coming soon" (placeholder) / "My Recipe Book" (live).

**Placeholder (`PlaceholderScreen`)**

- [DD3] Calm, intentional, no dead-end feel. Hierarchy: feature's Feather icon
  in `copper` at display size (visual anchor) → feature name (`type.h2`,
  Fraunces) → one calm head-chef line (`type.body`, `inkMuted`), sentence case,
  no emoji, e.g. "Recipe Lab is still in the kitchen — arriving soon." →
  `Eyebrow` section overline. NO CTA button (return via the header back).
- Microcopy register: calm head chef (matches Antoine voice). Never "Coming
  soon!", never food puns.

**Nested-stack header / back affordance**

- [DD4] `app/(tabs)/kitchen/_layout.tsx` Stack `headerShown: false`. Hub draws
  its own "Kitchen" title. Placeholder draws a minimal custom top row: copper
  back chevron (44pt tap target, `accessibilityLabel="Back to Kitchen"`) +
  section `Eyebrow`. No native header (would clash with Fraunces/copper, like
  ChatHeader avoids). Gesture back still works; the chevron makes it visible.

Motion: hub rows + placeholder use `FadeInDown.duration(240)` (per CLAUDE.md).
Press-scale 0.97 on rows and the back chevron.

## GSTACK REVIEW REPORT

| Review        | Trigger               | Why                             | Runs | Status | Findings                                                                                                           |
| ------------- | --------------------- | ------------------------------- | ---- | ------ | ------------------------------------------------------------------------------------------------------------------ |
| CEO Review    | `/plan-ceo-review`    | Scope & strategy                | 1    | clean  | SELECTIVE_EXPANSION; 3 proposals, 2 accepted (E1, E3), 1 deferred (E2); 0 critical gaps                            |
| Codex Review  | `/codex review`       | Independent 2nd opinion         | 0    | —      | not run                                                                                                            |
| Eng Review    | `/plan-eng-review`    | Architecture & tests (required) | 1    | clean  | FULL_REVIEW; 3 findings folded (D2 build-time gate, D3 guard placement, D4 route-leak); 0 critical gaps            |
| Design Review | `/plan-design-review` | UI/UX gaps                      | 1    | clean  | 6/10 → 9/10; 3 decisions (grouped inset rows, calm placeholder, headerShown:false custom header) + a11y/slop specs |
| DX Review     | `/plan-devex-review`  | Developer experience gaps       | 0    | —      | not run                                                                                                            |

- **CROSS-MODEL:** Three outside-voice passes total (Claude subagent — Codex not installed). CEO: D5 nested stack + E3 timing. Eng: confirmed `segments` match, caught hidden-tab route leak + T5/T7 conflict (→ D4), flagged headerShown (→ design review). Design: text-spec on the 3 focus areas resolved the headerShown gap.
- **VERDICT:** CEO + ENG + DESIGN CLEARED — implemented (commit `94cbfeb`).
- **DEVICE-VERIFIED (2026-06-17, Moto G86 Power):** tab bar persists on placeholder, custom copper back works (chevron + hardware back), 3 sections / 11 "Soon" rows render, no native header. Automated: tsc + lint clean, 194 jest tests.

NO UNRESOLVED DECISIONS
