/**
 * Shared icon prop type. Defined in its own module so consumers can import
 * the type without pulling in the full SVG icon set.
 */
export interface IconProps {
  size?: number;
  color?: string;
}
